#include "instruction.h"

struct instruction *create_instruction(){};

void get_instruction(struct instruction *p, char *word)
{
    struct instruction *p = NULL;
    switch (p->command) {
        case :

            
    
    }
}
